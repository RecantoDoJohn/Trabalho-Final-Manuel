<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <title>Relatório de Amigos</title>

  <style>
    body { font-family: Arial; font-size: 12px; }
    h1 { margin-bottom: 8px; }
    table { width: 100%; border-collapse: collapse; }
    th, td { border: 1px solid #ccc; padding: 6px; }
    th { background: #eee; }
  </style>
</head>
<body>

<h1>Relatório de Artistas</h1>

<table>
  <thead>
    <tr>
      <th>ID</th>
      <th>Nome</th>
      <th>Musicas</th>
    </tr>
  </thead>
  <tbody>
    <% artistas.forEach(amigo => { %>
      <tr>
        <td><%= amigo.id %></td>
        <td><%= amigo.nome %></td>
        <td>
          <% amigo.musicas.forEach(musica => { %>
            <li><%= musica.nome %> (<%= musica.genero %>)</li>
          <% }) %>
        </td>
      </tr>
    <% }) %>
  </tbody>
</table>

</body>
</html>
